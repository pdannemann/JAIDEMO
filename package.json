{
  "scripts": {
    "start": "servor --reload",
    "build": "mkdir -p dist && cp -r *.html *.js *.css dist/ && cp -r src dist/"
  },
  "dependencies": {
    "servor": "^4.0.2",
    "pdf-parse": "^1.1.1"
  },
  "type": "module"
}